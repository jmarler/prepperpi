# images/

pi-gen configs for producing prebuilt SD-card images (`prepperpi-<version>-pi4.img.xz`, `prepperpi-<version>-pi5.img.xz`). CI workflow in `.github/workflows/release.yml` consumes these to cut releases.
