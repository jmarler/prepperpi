# web/

Browser-facing SPAs:

- `landing/` — the page served at `http://prepperpi.local` and at every captive-portal redirect.
- `admin/` — the settings and content-management UI. Talks to `services/prepperpi-admin`.
