# prepperpi-updater

Download and bundle engine. Switches the Pi into "online mode" on demand (joins an upstream Wi-Fi while the AP is paused), pulls ZIMs / MBTiles / FEMA bundles via `aria2`, verifies checksums, and swaps them into place.
