# prepperpi-web

Caddy reverse proxy and the static landing SPA. Serves `/` (landing page), routes `/wiki` to kiwix-serve, `/maps` to TileServer GL, `/admin` to the FastAPI admin backend, and `/usb` to the USB file browser.
